# ── Use CPU-only PyTorch (keeps deploy size small on Render) ──
--extra-index-url https://download.pytorch.org/whl/cpu

# ── API Framework & Server ──
fastapi==0.109.2
uvicorn==0.27.0.post1
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# ── Database ──
SQLAlchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# ── Task Queue ──
celery==5.3.6
redis==5.0.1

# ── File Handling ──
python-multipart==0.0.9

# ── Image Forensics & Utilities ──
pillow==10.2.0
exifread==3.0.0
numpy>=1.24.0
qrcode[pil]==8.2

# ── AI / Gemini ──
google-generativeai>=0.4.0
wikipedia>=1.4.0

# ── ML / AI Text Detection (Transformer + Stylometry Ensemble) ──
torch>=2.1.0
transformers>=4.36.0

# ── Document Analysis ──
pdfplumber>=0.10.0
pytesseract>=0.3.10

# ── Domain Analysis ──
python-whois==0.8.0

# ── Cryptographic Signing ──
cryptography==42.0.2
